OSARETIN refresh — actions and next steps

Branch: refresh/all-activities-2026-08-11

What I created in this branch:

1) .github/workflows/osaretin-update-deps.yml
   - A workflow you can trigger manually (workflow_dispatch) that:
     - Runs `go get -u ./...` to update module versions (minor/patch updates where available).
     - Runs `go mod tidy` to clean up go.mod/go.sum.
     - Runs `go fmt ./...` and commits formatting changes.
     - Uses peter-evans/create-pull-request to open a PR named "chore: update Go dependencies (OSARETIN)" with the updates.
   - This workflow requires no extra secrets; it uses the built-in GITHUB_TOKEN.

2) This file (OSARETIN-REFRESH.md) explaining the change.

Next recommended steps (manual):
- Review the workflow file and, if acceptable, trigger it from the Actions tab -> OSARETIN: Update Go dependencies -> Run workflow.
- Wait for the workflow to complete and open the generated PR (if there are changes). Review CI and run tests locally if needed.
- Merge the generated PR after review.

Notes and safety:
- The workflow updates dependencies automatically; it may introduce compile/test failures. Review the PR carefully.
- I did NOT modify source files or go.mod directly in this commit to avoid breaking the repo without running `go`.
- If you want me to perform the updates directly in this branch and open a PR now, confirm and I will attempt to update go.mod and related files directly (but I cannot run `go` locally — changes would be best generated by the workflow above).

I am the GitHub Copilot Chat Assistant.